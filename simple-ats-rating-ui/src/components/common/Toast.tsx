/**
 * Toast通知组件
 * 基于@radix-ui/react-toast和shadcn/ui
 */

import { Toaster } from "@/components/ui/toaster"

/**
 * Toast通知组件
 * 用于在应用中显示全局通知消息
 */
export const Toast = () => {
  return <Toaster />
}

export default Toast
